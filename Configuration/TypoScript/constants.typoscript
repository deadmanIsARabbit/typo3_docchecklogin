plugin.tx_typo3docchecklogin {
    view {
        # Overwrite the template Root Path
        templateRootPath =
        # Overwrite the partial Root Path
        partialRootPath =
        # Overwrite the layout Root Path
        layoutRootPath =
    }
    settings {
        // This numeric parameter overrides the used Doccheck Login ID. Especially useful for working in multiple environments.
        loginOverrideId =
    }
}
